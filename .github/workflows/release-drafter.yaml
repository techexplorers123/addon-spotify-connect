---
name: Release Drafter

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main

jobs:
  workflows:
    uses: techexplorers123/workflows/.github/workflows/release-drafter.yaml@main
